# Архив рекомендаций (R1–P4)

> **Статус:** весь backlog **закрыт** (2026-06-05).

---

## Краткая сводка

| Диапазон | Тема |
|----------|------|
| R1–R38 | patch core, profiles, matchers |
| R39–R52 | orchestration: arch, impact, db, pipeline, index |
| P4 | index migrations/ADR, trustchain history, `init-consumer --with-arch-rules`, фаза `trustchain_history` |

Детали: [orchestration.md](../orchestration.md).

Пример pipeline: [manifests/engineering-pipeline.example.json](../manifests/engineering-pipeline.example.json).

**Тесты:** `pytest tests/ -q` (299+).

---

Новые идеи — issues или ADR в `docs/adr/`.
